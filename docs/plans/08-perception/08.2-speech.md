# 08.2 — Speech (Speech-to-Text)

**DONE (2026-08-15)** — `module STT` in `src/system/stt.jl` (backend detection: Ollama whisper via `/api/generate` → `whisper-cli` → `vosk-transcriber`; magic-byte MIME check WAV/MP3/OGG/FLAC; 100 MB size guard; `:external` error when no backend — never a guess; `record_clip(seconds)` live capture via `arecord`/`parecord` into an allowed dir), a `transcribe_audio` tool in `AgentTools` (capability `"audio"`), bridge routes `audio.transcribe` / `audio.record`, `ai.query` accepts `audio_file` (transcript injected as a user message), and TUI `/record` command (draft for correction). Tests: `test/stt_test.jl` (27). Full suite 4751 pass / 2 broken.

## Objective

Add a **speech-to-text tool** so Kamila can accept voice input, complementing the existing text-to-speech output (`src/ai/tts.jl:66-102`). Works headless (via the `06.1` daemon / bridge) so audio can be transcribed and acted upon.

## Current State

- `TTS.speak` writes text → audio (piper → spd-say → espeak fallbacks, `src/ai/tts.jl:78-101`). Input direction is absent.
- No microphone capture, no audio file handling, no STT dependency.
- `docs/KAMILA_MANUAL.md` references voice output conceptually but nothing records audio.

## Design

- **STT backends (priority order), behind `src/system/stt.jl` seam:**
  1. Ollama whisper (`whisper:large-v3-turbo` or similar) via `/api/generate` on audio bytes — truly local, no cloud.
  2. `whisper.cpp` (`whisper-cli`) if installed.
  3. `vosk` CLI if present, else explicit `:external` unavailable error.
- **New tool** `transcribe_audio(file_path) → {text, confidence?}`:
  - Validates path; accepts `.wav`, `.mp3`, `.ogg`, `.flac`; MIME/extension check.
  - Converts to 16 kHz mono wav for the model (ffmpeg if available; else raw passthrough).
  - Returns recognized text (and optional per-word timestamps for alignment).
- **Live capture (bridge/daemon):** optional block `audio.record` that records N seconds from microphone via `arecord`/`parecord`, saves to a temp wav in allowed dirs, then transcribes. Gated `:ask`.
- **Integration:** transcribed text is injected as a user message into `handle_ai_query` (`src/bridge.jl:284`), so voice → Kamila action uses the normal pipeline. `TTS.speak` already exists for the reply-side.
- **Draft flag:** transcription includes a confidence flag; TUI shows the recognized text for correction before sending (since local STT isn't perfect).

## Work Breakdown

| # | File | Change | Done-criteria |
|---|------|--------|---------------|
| 1 | `src/system/stt.jl` | New module: backend detection, `transcribe(file)` → text or `:external` error, format conversion. | Transcribes a fixture wav via mocked backend; returns error when none. |
| 2 | `src/ai/tools.jl` | Register `transcribe` tool. | Tool callable; permission-gated. |
| 3 | `src/ai/tts.jl` | (optional) capture live mic via `arecord` → `transcribe`. | `audio.record` route produces transcribed text. |
| 4 | `src/bridge.jl` | `transcribe`/`audio.record` routes; wire transcribed text into `ai.query` as a user message. | Transcribed text reaches the agent pipeline. |
| 5 | `tui/src/app.js` | Voice input button + draft-text correction view. | User can review STT text before send. |
| 6 | `test/stt_test.jl` | New: backend detect, transcribe (mock → text), unsupported format, no-backend error, permission gate. | All pass with seam mocked. |

## Acceptance Criteria

- [x] `transcribe` returns text for a fixture audio file via the mock backend.
- [x] Unsupported format → `:validation` error.
- [x] No STT backend → `:external` error, no hang/crash.
- [x] Transcribed text appears as a user message in-conversation.
- [x] Recording a clip (mocked `arecord`) yields a sendable draft.

## Risks & Mitigations

- **Risk:** Whisper models are large for local. **Mitigation:** backend gating; optional install; `transcribe` still works headless on existing files.
- **Risk:** Privacy of microphone audio. **Mitigation:** default `:ask` permission before recording; temp files in a gitignored dir; no cloud; clear post-recording deletion.
- **Risk:** Bad transcription. **Mitigation:** draft-and-confirm UI + confidence flag.

## Dependencies

- Depends on: `05.1` tool schemas, `02.2` permission, `06.1` daemon (headless capture).
- Informs: `08.3` (multimodal sessions).

## Definition of Done

- [x] CI is green (01.2 pipeline passes) as a precondition.
- [x] All acceptance criteria pass.
- [x] STT seam has a mocked-backend test + no-backend test.
- [x] Draft-correction path wired in TUI.